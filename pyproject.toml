[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "space-charge-sim"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "pyqt5",
    "mplcursors"
]
